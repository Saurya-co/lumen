{
  "name": "lumen-electron",
  "private": true,
  "version": "1.0.0",
  "description": "Lumen Study Browser — Electron shell (Chromium-based, lightweight, power-efficient)",
  "main": "dist/main.js",
  "scripts": {
    "//": "Run these from the project root, not from electron/. See electron/README.md.",
    "compile": "tsc main.ts preload.ts --outDir dist --module commonjs --target es2020 --esModuleInterop --skipLibCheck --resolveJsonModule",
    "dev": "LUMEN_DEV=1 electron dist/main.js",
    "start": "electron dist/main.js"
  },
  "dependencies": {},
  "devDependencies": {
    "electron": "^33.0.0",
    "electron-builder": "^25.1.8",
    "@types/node": "^22.0.0",
    "typescript": "^5"
  }
}
